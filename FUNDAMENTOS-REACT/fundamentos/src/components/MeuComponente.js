const MeuComponente = () => {
    return (
        <div>
            {/* Este parágrafo será exibido sempre que o componente for renderizado */}
            <p>Este Componente está sendo reaproveitado em vários lugares.</p>
        </div>
    );
}

// Exporta o componente para ser importado e usado em outros arquivos
export default MeuComponente;
